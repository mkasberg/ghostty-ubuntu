# Agent Notes

This is a packaging repository for an Ubuntu PPA for Ghostty. We also build zig
because we need a more recent version than Ubuntu offers.

Ghostty packaging source is at `ghostty/debian` and zig packaging source is at
`zig0.15/debian`. We also have some non-standard scripts at the root of this
repository.

